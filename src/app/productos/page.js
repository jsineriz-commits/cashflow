import ProductCatalog from '@/components/ProductCatalog/ProductCatalog';

export const metadata = {
  title: 'Catálogo y Precios | Cashflow PYMES',
};

export default function ProductosPage() {
  return <ProductCatalog />;
}
